---

name: nightly

# on:
#   schedule:
#     - cron: '0 4 * * *'
#   workflow_dispatch:

on: push

jobs:
  nightly:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Main
        run: |
          script/update.sh
